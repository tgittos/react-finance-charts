import * as components from './components';
import * as interfaces from './interfaces';
import { axes, figures, charts, utils, IChartFactory, createChart } from './lib';


export {
    // factories
    IChartFactory,
    createChart,
    // interfaces
    interfaces,
    // components
    components,
    // sub-libraries
    axes,
    figures,
    charts,
    utils
}